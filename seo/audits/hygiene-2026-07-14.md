# SEO Hygiene Audit — 2026-07-14

**Data source:** Manual crawl of the 6 production HTML pages in the repo (`index.html`,
`about.html`, `services.html`, `how-it-works.html`, `contact.html`, `questions.html`) plus
`robots.txt` and `sitemap.xml`. No GSC/crawler CSV export available — confidence is High for
static markup issues (directly verified in source), N/A for anything ranking/traffic-related.

**Site context:** see `seo/SITE.md`.

## Summary

| Severity | Count | Theme |
|---|---|---|
| High | 1 | Contact form doesn't submit (deferred, not fixed in this pass) |
| Low | 4 | Stale/incomplete metadata and missing structured data |
| Pass | 8 areas | Titles, metas, canonicals, robots, internal links, alt text, NAP consistency, sitemap coverage |

## Issue Table

| # | Issue | Type | Pages Affected | Current Value | Proposed Fix | Priority | Confidence |
|---|---|---|---|---|---|---|---|
| 1 | Contact form doesn't submit anywhere | Functional / conversion-blocking | contact.html:96 | `action="https://formspree.io/f/REPLACE_WITH_FORMSPREE_ID"` | Replace with the real Formspree form ID | High | High (directly verified) — **deferred by user, not fixed this pass** |
| 2 | `og:image` dimensions stale | Social preview | index.html:19-20, about.html, services.html, how-it-works.html, contact.html, questions.html (same block repeated) | `og:image:width=1100`, `og:image:height=934` | Update to `1400`/`933` to match the current `images/hero-ai.jpg` (swapped 2026-07-12, verified via `sips`) | Low | High |
| 3 | Sitemap `lastmod` frozen | Crawl signal | sitemap.xml (all 6 `<url>` entries) | `<lastmod>2026-04-30</lastmod>` on every entry | Update to each page's real last-edit date (all 6 pages show `2026-07-14` on disk) | Low | High |
| 4 | Missing LocalBusiness/structured data | Structured data | services.html, contact.html, how-it-works.html | No `application/ld+json` block (index.html and about.html have `LocalBusiness`; questions.html has `FAQPage`) | Add a matching `LocalBusiness` JSON-LD block to these 3 pages, copying the pattern already in index.html | Low | High |
| 5 | No dedicated `apple-touch-icon.png` | Icons | site-wide (`<head>` on every page) | Only `<link rel="icon" href="/favicon.svg">` present | Generate a 180×180 `apple-touch-icon.png` and add the `<link rel="apple-touch-icon">` tag | Low | High (already tracked in BACKLOG.md) |

## Pass — No Action Needed

| Area | Evidence |
|---|---|
| Page titles | Unique, branded, correct length on all 6 pages |
| Meta descriptions | Unique, no keyword-stuffing, all under ~160 chars |
| Canonical tags | Present, correct, self-referential on every page |
| Robots meta | `index, follow` on all 6 production pages; `hero-preview.html` (dev-only) correctly `noindex, nofollow` and not in sitemap |
| robots.txt | `Allow: /` + sitemap reference, no accidental blocking |
| Internal links | Every `href="*.html"` across all 6 pages resolves to a real, existing page — no broken links found |
| Image alt text | No `<img>` tag found without an `alt` attribute |
| NAP consistency | Phone `(919) 902-4150` / `tel:9199024150` identical on all 6 pages; "Raleigh, North Carolina" / "Raleigh and Wake County" consistent; no street address (intentional, service-area business) |
| Sitemap coverage | All 6 production pages present; dev-only `hero-preview.html` correctly excluded |

## Safety Notes

- This pass is read-only — no live-site changes were made from `/seo hygiene` itself; issues #2-#5
  are proposed fixes only.
- No redirects exist to break; `netlify.toml` only has one 301 (`/index.html` → `/`), unaffected by
  any of the above.
- No analytics/traffic data was invented — quick-wins-style ranking (click prize, striking-distance
  keywords) requires GSC or CSV export data, neither of which is connected. Run `/seo quick-wins`
  once Search Console access or a crawl/keyword export is available.
