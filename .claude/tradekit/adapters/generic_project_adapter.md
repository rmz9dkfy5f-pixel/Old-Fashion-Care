# Adapter — Generic Project

Default adapter for any repo not covered by a specific adapter.

- `/tk setup` once to record constraints, verification commands, and handoff expectations.
- Run the core loop: scan → decide → slice → implement → verify → handoff.
- If project state is ever unclear, stop and run `/tk scan`.
